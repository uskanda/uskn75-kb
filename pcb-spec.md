# 分割JIS 75% + Cirque 35mm — PCB設計仕様 rev 1.2

## 1. 基本諸元

| 項目 | 左半体 | 右半体 |
|---|---|---|
| キー数 | 43 + ロータリーエンコーダ3 | 52 |
| マトリクス | **6行 × 9列**（COL8は押込専用の疎な列） | 6行 × 9列 |
| 必要GPIO | 15 + シリアル2 + エンコーダ6 = **23** | 15 + SPI4 + DR1 + シリアル2 = **22** |
| MCU | Raspberry Pi Pico | Waveshare RP2040-Plus（マスタ） |
| キー領域 | 191.5 × 114.3 mm | 243.8 × 121.0 mm |
| 層構成 | 2層 / 1.6mm / HASL | 同左 |

> QMK の `MATRIX_COLS` は分割の左右で共通のため、**左右とも9列に統一**した（rev 1.2）。
> 左の COL8 は物理キーを持たず、ロータリーエンコーダの押込スイッチ3個のみが属する。

## 2. 基板外形

原点はキー領域の左上（u座標 0,0）。Y は下方向が正（KiCadと同じ）。

**左半体**（矩形）
```
(-32.5, -26) → (164.9, -26) → (164.9, 117.3) → (-32.5, 117.3)
```
外形 197.4 × 143.3 mm（エンコーダ列のぶん左へ29.5mm拡張）

**右半体**（トラックパッド部が下へ張り出す段付き形状）
```
(-15.0, -26) → (251.6, -26) → (251.6, 124.0) → (45.0, 124.0)
            → (45.0, 132.0) → (-15.0, 132.0)
```
外形 266.6 × 158.0 mm（rev 1.2: パッド開口の残り材料確保のため左端を −6.8 → −15.0 へ延長。開口左の材料は約9.5mmになる）

上端の Y=-26〜0 は **MCU・USB・TRRS を載せるための帯**です。ここが無いとPicoが物理的に置けません（キーピッチ19.05mmに対しPico幅21mm）。

## 3. トラックパッド開口

| 項目 | 値 |
|---|---|
| 中心座標 | X = **15.53** mm, Y = 101.44 mm（rev 1.2: Nキーとのクリアランス3.05mm確保のため0.19mm左へ） |
| 開口 | **Ø42 mm**（TM035035 の外形は現物実測で最終確定） |
| 取付 | 基板裏面からM2スペーサーで吊り、天面をプレート面と面一に合わせる |
| FPCコネクタ | 開口中心の下、(15.7, 138.0) 付近・**裏面実装** |

Ø42の丸抜きは JLCPCB のルーティング追加料金の対象（5mm超の円形溝）です。

## 4. ピンアサイン

### 右半体（RP2040-Plus / マスタ）
| 用途 | GPIO |
|---|---|
| ROW0..ROW5 | `GP2, GP3, GP4, GP5, GP6, GP7` |
| COL0..COL8 | `GP8, GP9, GP10, GP11, GP12, GP13, GP14, GP15, GP22` |
| SPI0 SCK | `GP18` |
| SPI0 MOSI(TX) | `GP19` |
| SPI0 MISO(RX) | `GP20` |
| SPI0 CS | `GP21` |
| Cirque DR | `GP26` |
| 分割シリアル TX / RX | `GP0` / `GP1`（**全二重**） |
| 未使用 | `GP16, GP17, GP27, GP28` |

SPI0 は RP2040 のハード割り当てに準拠（SCK=GP18 / TX=GP19 / RX=GP20 / CSn=GP21）。

### 左半体（Raspberry Pi Pico / スレーブ）
| 用途 | GPIO |
|---|---|
| ROW0..ROW5 | `GP2, GP3, GP4, GP5, GP6, GP7` |
| COL0..COL8 | `GP8, GP9, GP10, GP11, GP12, GP13, GP14, GP15, GP22` |
| 分割シリアル TX / RX | `GP0` / `GP1`（**全二重**） |
| ENC1 A/B | `GP16` / `GP17` |
| ENC2 A/B | `GP18` / `GP19` |
| ENC3 A/B | `GP20` / `GP21` |
| 未使用 | `GP26..GP28` |

分割シリアルは**全二重**とし、TRRS の4極を Tip=TX / Ring1=RX / Ring2=VBUS(5V) / Sleeve=GND に割り当てる。
**左右の基板間で TX と RX をクロス**させること（左のGP0→右のGP1、左のGP1→右のGP0）。
QMK設定: `SERIAL_DRIVER = vendor`（RP2040標準のPIOドライバ）＋ `SERIAL_USART_FULL_DUPLEX`。TX=GP0 / RX=GP1。PIO実装なのでピンはハードUARTに縛られない。

## 5. マトリクス割り付け

### 左半体（6×9）
| |COL0|COL1|COL2|COL3|COL4|COL5|COL6|COL7|COL8|
|---|---|---|---|---|---|---|---|---|---|
|**ROW0**|Esc|F1|F2|F3|F4|F5|F6|Home|—|
|**ROW1**|HanZen|N1|N2|N3|N4|N5|N6|BSpcL|—|
|**ROW2**|Tab|Q|W|E|R|T|DelL|—|**ENC1 押込**|
|**ROW3**|Caps|A|S|D|F|G|EnterL|—|**ENC2 押込**|
|**ROW4**|LShift|Z|X|C|V|B|EscL|—|**ENC3 押込**|
|**ROW5**|LCtrl|LGui|LAlt|Muhenkan|SpaceL|Fn1|—|—|—|

COL8（左・GP22）: 物理キーを持たない疎な列。ロータリーエンコーダの押込スイッチ
ENC1/2/3 のみが ROW2/ROW3/ROW4 に属する。

### 右半体（6×9）
| |COL0|COL1|COL2|COL3|COL4|COL5|COL6|COL7|COL8|
|---|---|---|---|---|---|---|---|---|---|
|**ROW0**|Ins|F7|F8|F9|F10|F11|F12|DelR|PrtSc|
|**ROW1**|BSpcI|N7|N8|N9|N0|Minus|Caret|Yen|BSpc|
|**ROW2**|DelI|Y|U|I|O|P|At|LBrc|Enter|
|**ROW3**|EnterI|H|J|K|L|Semi|Colon|RBrc|—|
|**ROW4**|N|M|Comma|Dot|Slash|Ro|RShift|Up|—|
|**ROW5**|SpaceR|Henkan|Kana|RAlt|App|RCtrl|Left|Down|Right|

## 5.5 ロータリーエンコーダ（左半体）

| ENC | 中心座標 (mm) | 回転 | プッシュSWのマトリクス位置 |
|---|---|---|---|
| ENC1 | X = −17.15, Y = 19.05 | A/B → GP16/GP17 | ROW2 × COL8 |
| ENC2 | X = −17.15, Y = 57.15 | A/B → GP18/GP19 | ROW3 × COL8 |
| ENC3 | X = −17.15, Y = 95.25 | A/B → GP20/GP21 | ROW4 × COL8 |

- 部品は **EC11互換（縦型・プッシュスイッチ付き）**。A/B相は GPIO 直結、コモンは GND
- **回転はマトリクスに参加させない**。プッシュスイッチのみ**新設COL8（GP22）**へ入れ、ダイオードは通常キーと同じ扱い。COL8の配線は基板左端に収まる
- 縦間隔 38.1mm（2u）。ノブ径20mmでも余裕がある
- QMK: `ENCODER_ENABLE = yes`、`ENCODER_RESOLUTION 4`、Vialで割り当てるなら `ENCODER_MAP_ENABLE = yes`
- エンコーダは**スレーブ側（左）**に載る。QMKの分割対応で問題なく動作するが、
  `encoder_update_user()` ではなく **エンコーダマップ**で定義する方が設定が単純

## 6. 回路設計ルール

- **ダイオード方向：COL2ROW**。スイッチ一端→COL、他端→ダイオードのアノード、カソード→ROW
- ダイオードは 1N4148W（SOD-123）、**裏面実装**。スイッチ中心から下方 5.1mm、90度回転
- Kailh ホットスワップソケットも**裏面**。スイッチはプレート側から挿す
- MCUは**直はんだせず、低背ピンソケット実装**とする（Pico系は20P×2列）。書き込み失敗・故障時に交換できるようにするため。全高を抑えたい場合は基板に切り欠きを設けて沈める
- 電源: **TRRSで渡すのは 5V（マスタの VBUS）**。スレーブ側は Pico の **VSYS** に入れ、3.3Vは各半体のオンボードレギュレータで各自生成する。**3.3Vを半体間で共有してはならない**（電圧降下とレギュレータの競合を招く）
- Cirque へはマスタ（右）の 3V3 出力から供給。**Cirqueは3.3V動作なのでレベル変換不要**
- SPIラインは 100mm以内に収め、SCK/MOSI/MISOは並走させすぎない
- **FPCコネクタの電源ピン直近にデカップリングを置く**：100nF（0805）×1 + 10uF（0805）×1。
  Cirqueはケーブル越しの3.3V負荷になるため、基板側にも入れておく
- TRRS は PJ-320A。**活線挿抜でショートするため、通電中の抜き差し厳禁**をシルクに明記すること
- ESD保護・PTCヒューズは MCUボード側に実装済みのため基板側は省略

## 7. スタビライザー

プレートマウント。対象は3箇所のみ。

| 位置 | サイズ |
|---|---|
| 左半体 LShift | 2.25u |
| 左半体 Space | 2.25u |
| 右半体 Enter | 縦2u（JIS/ISO） |

※ 1.75u（SpaceR / RShift）は対象外。MX互換スタビライザーに2u未満の流通サイズは存在せず、
  1.75uキーはスタビ無しが標準（rev 1.2 で「任意追加」の記述を撤回）。

## 8. 制作手順

1. `kle-left.json` / `kle-right.json` を **kbplacer**（KiCadプラグイン）へ読み込ませ、SW/Dを自動配置
2. 自動配置が使えない場合は `place-left.py` / `place-right.py` をスクリプトコンソールで実行
3. 外形（Edge.Cuts）を §2 の座標で描く
4. MCU・TRRS・FPCコネクタを上端の帯と §3 の位置に手配置
5. ネットリストは §5 のマトリクス表どおりに結線
6. **Cirque FPC 12ピンの信号割り当ては、必ず TM035035 のデータシートから転記すること**（本書では規定しない）

## 9. 未確定事項

| 項目 | 確定方法 |
|---|---|
| TM035035 の基板外形・取付穴 | 現物実測（STEP 0 で入手後） |
| FPC 12ピンの信号順 | データシート |
| FPCコネクタの接点面（上/下） | 選定したFFCケーブルのタイプに合わせる |
| パッド沈み込み量 | STEP 2 のモックで実測（0.5 / 1.0mm を試作） |
| MCU用低背ソケットの型番 | 全高の実測後に確定（通常品で干渉する場合のみ低背品） |
| FPCコネクタの接点面 | FH12（下接点）／FH12A（上接点）の両方を入手し、現物合わせで確定 |
