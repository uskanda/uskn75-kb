# 分割JIS 75% + ポインティングデバイス搭載キーボード — 引き継ぎ資料

最終更新: 2026-08-21 / 設計 rev 1.3 ／ PCB仕様 rev 1.2

このドキュメントは、チャットでの設計検討を Claude Code へ引き継ぐためのものです。
**未確定事項（§8）と禁止事項（§9）を先に読んでください。**

---

## 0. 一行サマリ

HHKB からの移行を目的とした、**PCB から自製する分割型 JIS 75% キーボード**。
右半体に Cirque 35mm 円形トラックパッド、左半体にロータリーエンコーダ3個を搭載。
有線・QMK(Vial)・部品は原則国内調達。設計は完了、**基板発注前**の段階。

---

## 1. 確定要件（ユーザー発話由来・変更不可）

| # | 要件 | 備考 |
|---|---|---|
| R1 | 分割型 | HHKBから移行したい |
| R2 | QWERTY / **JIS** / **row-staggered** / 75% | 外出時にノートPCを使うため配列互換性が最優先 |
| R3 | ファンクション行・矢印キーを含む | |
| R4 | 分割面の内側に増設キー列。Enter を押しやすく | |
| R5 | 有線接続 | 無線対応は不要 |
| R6 | 部品は原則国内入手 | Cirque のみ例外（国内流通なし） |
| R7 | ポインティングは**小型**・キーから指を離さず・**ホームポジションの手が当たらない**位置 | |
| R8 | 左右クリック相当が必要 | |
| R9 | 右Shiftは JIS標準 1.75u、ろ は 1u | 刻印キーキャップ互換を優先した結果 |
| R10 | 矢印キーは独立島として右へ分離。全体幅の増加は許容 | |
| R11 | 右半体最下段の左端に Space を追加 | |
| R12 | 左端にロータリーエンコーダ 縦1列 ×3 | |
| R13 | 左右の分離幅は肩が開く程度（150〜250mm） | |

---

## 2. 確定仕様

### 2.1 全体

| 項目 | 左半体 | 右半体 |
|---|---|---|
| キー数 | 43 + エンコーダ3 | 52 |
| マトリクス | **6行 × 9列** | **6行 × 9列** |
| 必要GPIO | 23 | 22 |
| MCU | Raspberry Pi Pico（**スレーブ**） | Waveshare RP2040-Plus（**マスタ**・USB-C） |
| キー領域 | 191.5 × 114.3 mm | 243.8 × 121.0 mm |
| 基板外形 | 197.4 × 143.3 mm | 266.6 × 158.0 mm |
| 層構成 | 2層 / 1.6mm / HASL | 同左 |

**マトリクスは左右で9列に統一している。** QMK の `MATRIX_COLS` は分割の左右で共通のため。
左の COL8 は物理キーを持たず、エンコーダ押込スイッチ3個のみが属する疎な列。

### 2.2 ポインティング

- **Cirque TM035035-2024-002**（有効径35mm、フラットオーバーレイ）
- 位置: 右内側列、**Enter の真下**（Z行〜最下段）。中心 X=15.53mm / Y=101.44mm
- 接続: **SPI**（モジュール既定モード。R1 実装でSPI、未実装でI2C）
- 開口 Ø42mm、基板裏面からM2スペーサーで吊り、プレート面と面一
- **追加オーバーレイは被せない**（-002 は既にフラットオーバーレイ付き。二重化は検出距離のマージンを食う）

### 2.3 操作系

| 操作 | 割り当て |
|---|---|
| 左クリック | パッドをタップ ／ 右手 J |
| 右クリック | 右手 L |
| 中クリック | 右手 K |
| スクロール | 左手 Fn1 ホールド中のなぞり |
| ドラッグ | ダブルタップ＆ホールド |

パッドに触れた瞬間に自動でマウス層へ切替（`POINTING_DEVICE_AUTO_MOUSE_ENABLE`）。
**Fn2 は廃止済み**（マウス層が自動化されたため不要になった）。

### 2.4 内側増設列

| 行 | 左 | 右 |
|---|---|---|
| F行 | Home | Insert |
| 数字 | BackSpace | BackSpace |
| Q行 | Delete | Delete |
| ホーム | **Enter** | **Enter**（H の左） |
| Z行 | Esc | ← トラックパッドが2行占有 |
| 最下段 | Fn1 | ← |

従来の Enter / BS / Esc は右端に温存（ノートPC併用時の混乱回避）。

### 2.5 エンコーダ

| ENC | 中心座標(mm) | A/B | 押込マトリクス |
|---|---|---|---|
| ENC1 | X=−17.145, Y=19.05 | GP16/GP17 | ROW2 × COL8 |
| ENC2 | X=−17.145, Y=57.15 | GP18/GP19 | ROW3 × COL8 |
| ENC3 | X=−17.145, Y=95.25 | GP20/GP21 | ROW4 × COL8 |

EC11互換（縦型・プッシュ付き）。回転はマトリクスに参加させない。縦間隔38.1mm。
デフォルト割当案: EC1=音量/ミュート、EC2=スクロール/中クリック、EC3=任意。

### 2.6 ピンアサイン

**右半体（RP2040-Plus / マスタ）**
```
ROW0-5      : GP2, GP3, GP4, GP5, GP6, GP7
COL0-8      : GP8, GP9, GP10, GP11, GP12, GP13, GP14, GP15, GP22
SPI0        : SCK=GP18, MOSI=GP19, MISO=GP20, CS=GP21
Cirque DR   : GP26
分割シリアル : TX=GP0, RX=GP1（全二重）
未使用      : GP16, GP17, GP27, GP28
```

**左半体（Pico / スレーブ）**
```
ROW0-5      : GP2, GP3, GP4, GP5, GP6, GP7
COL0-8      : GP8, GP9, GP10, GP11, GP12, GP13, GP14, GP15, GP22
ENC1/2/3 A/B: GP16/GP17, GP18/GP19, GP20/GP21
分割シリアル : TX=GP0, RX=GP1（全二重）
未使用      : GP26, GP27, GP28
```

TRRS 4極 = Tip:TX / Ring1:RX / Ring2:**VBUS(5V)** / Sleeve:GND。
**左右基板間で TX と RX をクロスさせること。**

### 2.7 電源

- TRRS で渡すのは **5V（マスタのVBUS）**。スレーブは Pico の **VSYS** で受ける
- 3.3V は各半体のオンボードレギュレータで各自生成。**3.3Vの半体間共有は禁止**
- Cirque へはマスタ(右)の 3V3 出力から供給。3.3V動作なのでレベル変換不要

### 2.8 ファームウェア

- QMK（Vial fork）
- `MASTER_RIGHT` を定義。**`SPLIT_POINTING_ENABLE` は不要**（パッドがマスタ直結のため）
- `SERIAL_DRIVER = vendor`（RP2040標準のPIOドライバ）＋ `SERIAL_USART_FULL_DUPLEX`
- `POINTING_DEVICE_DRIVER = cirque_pinnacle_spi`
- `CIRQUE_PINNACLE_TAP_ENABLE` / `POINTING_DEVICE_AUTO_MOUSE_ENABLE`
- `ENCODER_ENABLE = yes` / `ENCODER_RESOLUTION 4` / `ENCODER_MAP_ENABLE = yes`
- JIS: `INT1`〜`INT5`（ろ / かな / ¥ / 変換 / 無変換）。OS側も日本語106設定

---

## 3. 回路設計ルール

- ダイオード方向 **COL2ROW**。1N4148W（SOD-123）、**裏面実装**、スイッチ中心から下方5.1mm・90度回転
- Kailh ホットスワップソケットも裏面。スイッチはプレート側から挿す
- **MCUは直はんだせずピンソケット実装**（書き込み失敗・故障時の交換性確保）
- **FPCコネクタの電源ピン直近にデカップリング**: 100nF(0805)×1 + 10uF(0805)×1
- SPIラインは100mm以内。SCK/MOSI/MISO を並走させすぎない
- TRRS は PJ-320A。**通電中の抜き差し厳禁**をシルクに明記
- ESD保護・PTCヒューズは MCUボード側に実装済みのため基板側は省略

### スタビライザー（プレートマウント・3箇所のみ）

| 位置 | サイズ |
|---|---|
| 左 LShift | 2.25u |
| 左 Space | 2.25u |
| 右 Enter | 縦2u（JIS/ISO） |

**1.75u（SpaceR / RShift）は対象外。** MX互換スタビに2u未満の流通サイズは存在せず、
1.75uキーはスタビ無しが標準。

---

## 4. 成果物ファイル

| ファイル | 内容 |
|---|---|
| `pcb-spec.md` | PCB設計仕様の正本。外形座標・開口・ピン・マトリクス表 |
| `kle-left.json` / `kle-right.json` | KLE形式。**kbplacer**（KiCadプラグイン）に読ませてSW/Dを自動配置 |
| `place-left.py` / `place-right.py` | kbplacerが使えない場合の pcbnew 直接配置スクリプト |
| `matrix-left.csv` / `matrix-right.csv` | 全95キーの座標・行列位置・ラベル |
| `encoders-left.csv` | エンコーダ3個の座標とピン割当 |
| `split-jis75-tb.html` | 設計案の全体像。配列図・設置イメージ・BOM・調達先（ブラウザで開く） |

**注意: `place-left.py` / `place-right.py` の座標は rev 1.2 の変更（左COL8新設・
パッド0.19mm左シフト・右基板左端延長）を反映していない。**
再生成が必要（生成ロジックは `pcb-spec.md` §2〜§5 が正）。

---

## 5. 調達計画

### Mouser Japan（DDP・関税消費税込み）
| 品目 | 品番 | 数 |
|---|---|---|
| Cirque 35mm パッド | `355-TM0350352024-002` | 2〜3 |
| FPCコネクタ 12P/0.5mm 下接点 | `798-FH12-12S-0.5SH55` | 4 |
| FPCコネクタ 12P/0.5mm 上接点 | FH12A-12S-0.5SH(55) | 4 |
| タクトスイッチ | `667-EVQP7C01P` | 4 |

送料無料ラインは6,000〜7,500円（変動、要カート確認）。

### 秋葉原（現地調達）
- **遊舎工房**: スイッチ95、Kailhソケット95、スタビ3、PJ-320A×2、TRRSケーブル、キーキャップ、プレート加工
- **秋月電子**: Pico、1N4148W×200、ピンソケット、デカップリングコンデンサ
- **千石電商**: FFCケーブル（`EEHD-04HL` 100mm / `7BKT-RPH7` 150mm、A・B両タイプ）、EC11、ノブ

### 通販
- **スイッチサイエンス**: Waveshare RP2040-Plus（秋葉原店頭は不確実）
- **PCB製造**: JLCPCB / Elecrow。**5枚は最小ロットで予備ではない**

### 概算
約 37,000〜66,000円（キーキャップと基板の振れ幅が支配的）

---

## 6. 制作手順

1. **STEP 0** — Mouser発注（型番末尾 **-002** 厳守）+ 秋葉原で現地調達。キーキャップ方針確定
2. **STEP 1** — パッド単体を Pico に繋いでマウスとして動かす。**加速カーブとタップ判定の詰めがこの案の山場**。基板を起こす前にここで納得する
3. **STEP 2** — 開口位置・沈み込み（0.5/1.0mm）のモックを Bambu Lab A1 で出力。**N を打つ指が誤反応しないか**を実測
4. **STEP 3** — **右半体のみ先に試作**。JISキーコードと自動マウス層の検証はここで完結（左は既存HHKBを併用して評価可能）
5. **STEP 4** — 左半体を起こして通し検証 → ケースを本番出力

**v1→v2 で2回発注する前提で予算を見ること。**

---

## 7. 設計判断の根拠（変更を検討する際に読む）

| 判断 | 理由 |
|---|---|
| 静電容量無接点を採用しない | ラバードーム＋コニックスプリングの安定調達がほぼ不可能。MX互換＋静音タクタイル＋テープ/フォームMODで代替 |
| トラックボールでなくトラックパッド | R7（手が当たらない）を満たすには面一である必要がある。球は必ず突出する |
| ポインティングスティックを採用しない | モジュールの単体販売が存在せず、ThinkPadからの摘出が唯一の入手経路 |
| 23mmでなく35mm | この位置ならキーを1つも減らさずに35mmが入る。23mmは加速カーブへの依存が強い |
| Cirque Gen6 を待たない | 円形パッドのGen6sは依然シングルタッチで性能面の利得がない。QMKドライバ不在、単体モジュールはCirque直販のみ、FFCが6ピンで非互換 |
| USBを右（マスタ）へ | パッドがマスタ直結になり `SPLIT_POINTING_ENABLE` と保護回路が不要になる |
| エンコーダを左へ | 右は22本使用済みで、3個足すと28本となりGPIOを超える。左は遊休GPIOがあった |
| 単純マトリクス（Duplex-Matrixにしない） | Duplex は Pro Micro のピン不足対策。RP2040ではピンが足りているので複雑化する理由がない |
| 3Dプリントのキーキャップは常用しない | ステム十字溝の許容誤差±0.05mmに対しFDMの精度が足りず、破断点になる。モック用と入手困難キーの穴埋めには積極活用 |

---

## 8. 未確定事項（Claude Code 側で確定させる）

| # | 項目 | 確定方法 |
|---|---|---|
| U1 | **Cirque FPC 12ピンの信号順** | **必ず TM035035 データシートから転記。推測での結線は破壊に直結** |
| U2 | TM035035 の基板外形・取付穴位置 | 現物実測（厚みは5.61mmとデータシートに記載あり） |
| U3 | FPCコネクタの接点面（上/下）の最終選択 | モジュールの吊り向きとFFCタイプの組み合わせで図面上確定可能 |
| U4 | パッド沈み込み量 | STEP 2 のモックで実測 |
| U5 | キーキャップ方針（JIS刻印セット / 無刻印） | Z行はJIS109と同一寸法構成なので**刻印セットでも組める**。内側列と右Space(1.75u)のみ汎用サイズで補う |
| U6 | N キーとのクリアランス | 現在 3.05mm（rev1.2でパッドを0.19mm左へ寄せて確保済み）。実機で要確認 |
| U7 | QMK分割エンコーダの片側のみ搭載構成 | `ENCODERS_PAD_A_RIGHT` を空定義できるか実機確認 |
| U8 | MCU用低背ソケットの型番 | 全高実測後に確定 |

---

## 9. やってはいけないこと

- **U1 を推測で埋めない。** データシート必須
- **キーキャップを買う前にキー幅を確定しない。** 順序は「キャップ→PCB」
- **Cirque の型番末尾を -002 以外にしない。** -003は曲面、-001は粘着で面一埋め込み不可
- **TRRSを通電中に抜き差ししない。** VBUS(5V)が乗っているためショートする
- **3.3Vを半体間で共有しない**
- Mouser で FFC / Pico / スイッチ類を買わない（国内の方が安いか、フットプリントが合わない）

---

## 10. 将来の拡張余地（基板発注前なら安価に仕込める）

- **FPCコネクタを小さな子基板に分離**（20mm角をメイン基板と同時に面付け）。
  Cirque世代交代（Gen6sは6ピンFFC）やトラックボール化に対応しやすくなる
- **GP20/GP21 に 4.7kΩ プルアップの未実装フットプリント**。RP2040のI2C0 SDA/SCLと一致するため、I2C化への備えになる
- **SPI4本＋MOTION1本のピンヘッダパッドを空きスペースに出す**。
  トラックボール（**PMW3360DM-T2QU** + LM19-LSI、34mm球、ルビー2.5mm×3）へ差し替える場合の受け口。
  ただし球は必ず突出するため、**現在の位置のままでは R7 を満たせない**。25mm球にするか右親指位置へ移す再設計が要る
- 開口とマウント穴を **35/40mm 互換**で切っておく
