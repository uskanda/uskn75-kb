# uskn75-kb

分割型 JIS 75% + Cirque 35mm トラックパッド搭載キーボード — my dream keyboard

HHKB からの移行を目的に、**PCB から自製する分割型 JIS 75% キーボード**。
右半体に Cirque 35mm 円形トラックパッド、左半体にロータリーエンコーダ3個、
最下段の下に親指Enterを左右1個ずつ。
有線・QMK(Vial)・部品は原則国内調達。

---

## 現在のステータス

| | 状態 |
|---|---|
| 設計 | **rev 1.7**（パッドの吊り方を確定。座標は rev 1.5 のまま） |
| PCB仕様 | **rev 1.3**（マトリクス 6×10 / 外形・パッド中心を更新） |
| KiCad 用データ | **生成済み**（rev 1.3 座標。KLE / マトリクスCSV / 配置スクリプト） |
| **STEP 0 調達** | **Mouser 着荷済み（2026-08-28）**。秋葉原（遊舎工房・秋月・千石）とスイッチサイエンスは未着手。**買い物を止める未確定は解消済み**（残るのは店頭在庫の確認 → `procurement.md` §0.5） |
| STEP 1〜4 | 未着手。**STEP 1 は着手可**（モジュール裏面のテストパッドへ直結できるので FFC も変換基板も要らない） |
| PCB 発注 | **未実施**。基板外形と物理配置は再検討予定のため、見積も取り直す |

### rev 1.7 で変えたこと（2026-09-06）

**現物確認で TM035035 に取付穴が存在しないことが分かり、パッドの吊り方（U9）が確定した。**
裏面にあるのは縁の切り欠き3か所と12時の小穴だけで、**表面側はオーバーレイが全面を覆っている**。
ネジがモジュールを貫通できないため、rev 1.6 まで書いていた「裏面から M2 スペーサーで吊る」は成立しない。

| 変更 | 内容 |
|---|---|
| 吊り方 | **プレート天面のリップ（内径 Ø33.0）で上から受け、下から受け座で押し上げる**。ネジはモジュールを貫通しない |
| 開口 | **PCB 側 Ø42（据え置き）と プレート側 Ø35.3 貫通 + リップ Ø33.0 に分離**。役割が違うので同じ数字で書かない |
| プレート材 | **非導電（PETG 自製 / アクリル）が必須**になった。リップがオーバーレイの外周を覆うため |
| 沈み込み（U4） | **リップの厚みそのもの**になった。0.5 / 1.0mm を刷り分けて実測する |
| キーキャップ | プロファイルを **行別 Cherry / OEM** で暫定確定。行6（親指Enter）は行5と同じ |

座標・外形・マトリクスは動いていないので **PCB仕様 rev は 1.3 のまま**。

### rev 1.5 で変えたこと（2026-09-02）

**狙いは、親指の定位置を Space から 無変換／変換 へ移すこと。** そのために次を同時に動かした
（1つだけでは成立しない。根拠は `pcb-spec.md` §11）。

| 変更 | 内容 |
|---|---|
| 親指Enter | 最下段の下に **1.5u を左右1個ずつ**新設。左は無変換の真下、右は変換の真下 |
| H の左の Enter | **廃止**。R4「Enter を押しやすく」は親指行が引き継ぐ |
| トラックパッド | その跡地へ移動。中心 (15.53, 101.44) → **(6.48, 76.20)**。ホーム行〜Z行を占有 |
| 左最下段 | Fn1 を左Ctrl の左へ。Ctrl 以降が 1.25u 右へ |
| 右最下段 | パッドが上がって空いた行を **2u 左へ**。矢印島も 0.8u 左へ（右Shift 1.75u に阻まれこれが上限） |
| 右内側列 | Ins / BS のさらに左に**無刻印2キー**を追加（割当は Vial で後決め） |
| マトリクス | 物理行0/1 が各10キーになり **6×9 → 6×10**。GPIO 左23→24 / 右22→23 |
| 基板外形 | 左右とも**親指Enterぶんが下へ張り出す段付き**。左 197.4×162.35 / 右 260.4×162.35 |

**受け入れた代償（`pcb-spec.md` §11 に記録）:**

- **最下段だけ JIS109 との位置互換を失う。** 英数ブロック・Z行・右Shift 1.75u は無傷なので R2 / R9 自体は維持
- **クリアランスの弱点が N から H へ移った。** N は 3.05 → 12.6mm に緩んだが、H が 3.05mm になった。
  H は**ホーム行**なので、自動マウス層の誤反応リスクは rev 1.4 より高い（**U6** で実測）

---

次にやること:

1. **U2③（全高の基準面）を実測する（最優先）。** 5.5mm がオーバーレイ面からの値か基板上面からの値かで、
   **受け座の高さ**——つまりパッド天面がプレート面のどこに来るか——が決まる。
   ①取付穴は解決（穴は存在しない）、②オーバーレイ外径は**設計上不要になった**（未実測。`pcb-spec.md` §9 U2）
2. ~~開口 Ø42mm と吊り方を決める~~ → **確定（rev 1.7）。** 取付穴が存在しないため、
   **プレート天面のリップで上から受け、下から受け座で押し上げる**方式にした。
   開口は **PCB 側 Ø42 / プレート側 Ø35.3 + リップ Ø33.0** に分離（`pcb-spec.md` §3 / §9 U9）
3. **STEP 1（パッド単体検証）は着手できる。** モジュール裏面のテストパッドへ直結すれば
   FFC も変換基板も要らない。要るのは MCU だけ。**この案の山場（加速カーブとタップ判定）を
   基板より先に潰せる**
4. **U1 は転記済み**（`pcb-spec.md` §4「Cirque トラックパッド FPC」）。
   残りは同梱データシート／Mouser 掲載 v1.2 との突合のみ。**回路図はこの表で着手できる**
5. **U3 は FFC 待ち。** モジュール側の向きは確定済み、基板側の FH12 / FH12A の選択は
   千石で A/B 両タイプの FFC を買ってから現物合わせ
6. ~~U5（キーキャップ方針）を決める~~ → **確定。無刻印（2026-09-03）+ 行別 Cherry/OEM（2026-09-06 暫定）。**
   行6（親指Enter）は行5と同じプロファイルで買う。買う数は `procurement.md` §2E の行別表。
   **キャップは後から買い直すだけで差し替えできる**（縦2u を除く → U11）
7. **秋葉原ぶんを1回で買い切る。** **買い物を止める未確定はもう無い。**
   残る当日前の作業は **`procurement.md` §0.5 の店頭在庫確認**（スイッチ110個・1u 無刻印92個）だけ。
   §2 のチェックリストで買う。**スイッチプレートは当日買わない**（自製に倒したため）
8. **U6（パッドとホーム行のクリアランス）を STEP 2 のモックで実測する。** rev 1.5 で難度が上がった
9. **U10（FPCコネクタとホットスワップソケットの干渉）を KiCad 上で確認する。**
   パッドが上がった結果、コネクタの置き場がキーの下へ移った
10. **U11（縦2u のステム位置）を実測する。** 無刻印の ISO/JIS Enter を買って帰ったら、
    ステムが現行データの縦2u中央 `(173.831, 57.150)` と合うか測る。**ここが PCB 発注の前提**

なお **基板外形と物理配置はもう少し詰める予定**（別途検討）。
`pcb-spec.md` §2 の外形と、そこから生成した座標データは**暫定として扱うこと**。

---

## リポジトリ構成

| ファイル | 役割 |
|---|---|
| `pcb-spec.md` | **設計の正本**。確定要件・基板外形座標・開口・ピンアサイン・マトリクス表・未確定事項・判断根拠・禁止事項 |
| `split-jis75-tb.html` | 設計案の全体像（配列図・設置イメージ・BOM・調達先）。ブラウザで開く |
| `procurement.md` | **STEP 0 の店舗別チェックリスト**。キーキャップ必要数・買う前の禁止事項 |
| `CLAUDE.md` | Claude Code 向けの作業規約 |
| `README.md` | このファイル |

### KiCad 用の成果物（生成物・手で編集しない）

| ファイル | 内容 |
|---|---|
| `kle-left.json` / `kle-right.json` | KLE形式。**kbplacer**(KiCadプラグイン)でSW/Dを自動配置する用。左上ラベルが `row,col`（VIA互換） |
| `matrix-left.csv` / `matrix-right.csv` | 全98キー（左44・右54）の座標・**論理行列位置と物理行**・GPIO・ラベル・ダイオード位置・スタビ |
| `encoders-left.csv` | エンコーダ3個の座標とピン割当・押込のマトリクス位置 |
| `keycap-sizes.csv` | キーキャップのサイズ別数量（U5 キーキャップ方針の判断材料） |
| `place-left.py` / `place-right.py` | kbplacer が使えない場合の pcbnew 直接配置スクリプト |
| `tools/gen-layout.py` | 上記の生成元 |

```
python3 tools/gen-layout.py
```

`tools/gen-layout.py` は数値を一切持たず、`split-jis75-tb.html` の配列と `pcb-spec.md`
§1/§3/§4/§5/§5.5/§5.6 を突き合わせて生成する。**食い違いがあれば出力せず停止する**ので、
配列や仕様を触ったら必ず実行すること。

---

## 仕様サマリ

| 項目 | 左半体 | 右半体 |
|---|---|---|
| キー数 | 44 + エンコーダ3 | 54 |
| マトリクス | 6行 × 10列（COL8は押込専用 / COL9は未接続） | 6行 × 10列 |
| GPIO | 24 | 23 |
| MCU | Raspberry Pi Pico（スレーブ） | Waveshare RP2040-Plus（マスタ・USB-C） |
| 基板外形 | 197.4 × 162.35 mm（段付き） | 260.4 × 162.35 mm（段付き） |
| 層構成 | 2層 / 1.6mm / HASL | 同左 |

物理行は7段（最下段の下に親指Enter）だが論理 ROW は6段。ずれるキーの対応は `pcb-spec.md` §5.6。

- **ポインティング**: Cirque `TM035035-2024-002`（モジュール外径 Ø35.0mm・センサ有効径 Ø31.0mm）／ SPI 接続 ／
  右内側列 **H の左**、中心 X=6.48 / Y=76.20 mm ／ **開口と吊り方は `pcb-spec.md` §3 が正本**
- **エンコーダ**: EC11互換 ×3、左端に縦1列（Y = 19.05 / 57.15 / 95.25、X = −17.15 mm）。
  回転は GPIO 直結、押込のみ新設 COL8(GP22) へ
- **半体間**: TRRS 4極（Tip=TX / Ring1=RX / Ring2=VBUS 5V / Sleeve=GND）、全二重、TX/RX クロス
- **ファーム**: QMK(Vial fork) / `MASTER_RIGHT` / `SERIAL_DRIVER = vendor` /
  `cirque_pinnacle_spi` / `POINTING_DEVICE_AUTO_MOUSE_ENABLE` / JIS は `INT1`〜`INT5`

詳細は `pcb-spec.md`、判断の根拠は同 §11。

---

## 制作ステップ

0. **調達** — Mouser（済）+ 秋葉原・スイッチサイエンス（未）。キーキャップは**無刻印で確定済み**
1. **パッド単体検証** — Pico に繋いでマウスとして動かす。**加速カーブとタップ判定がこの案の山場**。基板を起こす前に納得する
2. **モック** — 開口位置と沈み込み（0.5 / 1.0mm）を Bambu Lab A1 で出力。**H に指を置いただけで誤反応しないか**を実測（U6。rev 1.5 で対象が N から H へ移った）
3. **右半体のみ試作** — JISキーコードと自動マウス層の検証はここで完結（左は既存 HHKB を併用して評価可能）
4. **左半体** — 通し検証 → ケース本番出力

**v1→v2 で2回発注する前提**で予算を見ること。概算 37,000〜66,000円。

---

## 触る前に読むこと

`pcb-spec.md` §9（未確定事項 U1〜U11）と §12（やってはいけないこと）。特に:

- **Cirque FPC 12ピンの信号順を推測で埋めない。** データシート必須。破壊に直結する
- **キーキャップを買う前にキー幅を確定しない。** 順序は「キャップ → PCB」
- **型番末尾は `-002` 厳守。** -003 は曲面、-001 は粘着で面一埋め込み不可
- **TRRS の活線挿抜厳禁。** VBUS(5V) が乗っている
- **3.3V を半体間で共有しない**
