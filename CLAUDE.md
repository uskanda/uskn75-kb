# CLAUDE.md

自作キーボード **uskn75-kb**（分割JIS 75% + Cirque 35mm トラックパッド）のリポジトリ。
コードベースではなく**ハードウェア設計の資料リポジトリ**である。ビルド・テスト・CI は存在しない。

言語は**日本語**。ドキュメントもコミットメッセージも日本語で書く。

---

## 1. ドキュメントの正本（どれを信じるか）

| 対象 | 正本 |
|---|---|
| 要件・確定仕様・設計判断の根拠・未確定事項・禁止事項 | `HANDOFF.md` |
| 基板外形座標・開口・ピンアサイン・マトリクス | `pcb-spec.md` |
| 現在の進捗ステータス | `README.md` |
| 配列図・設置イメージ・BOM・調達先 | `split-jis75-tb.html` |

**矛盾を見つけたら勝手にどちらかへ寄せず、ユーザーに確認する。**
数値が食い違う箇所は rev の取り違えである可能性が高い（HANDOFF は設計 rev 1.3 / PCB仕様 rev 1.2 / HTML は v1.3 だが一部 §9 見出しに rev 1.0 の記載が残っている）。

`HANDOFF.md` はチャットから Claude Code への引き継ぎ記録である。
**進捗の更新は `README.md` の「現在のステータス」に書く**。HANDOFF は設計内容の変更時にのみ触る。

---

## 2. 絶対に守ること

`HANDOFF.md` §9 の再掲。これらは要件ではなく**破壊・手戻りに直結する制約**である。

- **Cirque FPC 12ピンの信号順を推測で書かない。** TM035035 のデータシートからの転記のみ。
  「たぶんこの順」で回路図を書くのは禁止。未確定なら未確定と明記して止める
- **Cirque の型番末尾は `-002`。** -003（曲面）/ -001（粘着）は面一埋め込みに使えない
- **キーキャップの寸法を確定する前に PCB を発注しない。** 順序は「キャップ → PCB」
- **TRRS は活線挿抜厳禁**（VBUS 5V が乗る）。シルクに明記する
- **3.3V を半体間で共有しない。** 渡すのは 5V のみ、3.3V は各半体で生成
- Mouser で FFC / Pico / スイッチ類を買わない（国内が安いかフットプリントが合わない）

## 3. 変更してはいけない確定要件

`HANDOFF.md` §1 の R1〜R13 はユーザー発話由来で**変更不可**。
「こうした方が良いのでは」と設計を最適化する前に、その提案が R1〜R13 のどれかを崩さないか確認する。
特に崩しがちなもの:

- **R2: JIS / row-staggered / 75%** — 外出時のノートPC互換性が最優先。列スタガや ANSI 化の提案はしない
- **R7: ポインティングは面一** — トラックボール化の提案は R7 を満たさない（球は必ず突出する）
- **R9: 右Shift 1.75u / ろ 1u** — 刻印キーキャップ互換のため。ここを動かすと JIS 刻印セットが使えなくなる

`HANDOFF.md` §7 に「なぜその選択をしなかったか」が判断ごとに書いてある。
**代替案を出す前に §7 を読む**。そこで既に却下された案を再提案しない
（静電容量無接点 / トラックボール / ポインティングスティック / 23mmパッド / Gen6 / USB左置き / エンコーダ右置き / Duplex-Matrix / 3Dプリントキーキャップ常用）。

---

## 4. 未確定事項の扱い

`HANDOFF.md` §8 の U1〜U8 が未確定。それぞれ**確定方法が決まっている**（データシート / 現物実測 / モック試作 / 実機確認）。

- 未確定項目を**推測で埋めた成果物を作らない**。値が要るなら「U◯ 未確定のため保留」と明記する
- 確定したら `HANDOFF.md` §8 と `pcb-spec.md` §9 の**両方**を更新する（同じ表が二重管理になっている）
- U1（FPC信号順）と U2（外形・取付穴）は Mouser 到着待ちのブロッカー。ここが埋まるまで回路図・フットプリントは描けない

---

## 5. 未生成の成果物

`HANDOFF.md` §4 が挙げるファイルのうち、リポジトリに存在するのは `pcb-spec.md` と `split-jis75-tb.html` のみ。
以下は**未生成**であり、生成する場合は `pcb-spec.md` §2〜§5 を唯一の入力とする:

`kle-left.json` / `kle-right.json` / `place-left.py` / `place-right.py` /
`matrix-left.csv` / `matrix-right.csv` / `encoders-left.csv`

HANDOFF §4 に「`place-*.py` の座標は rev 1.2 の変更（左COL8新設・パッド0.19mm左シフト・右基板左端延長）を
反映していない」という注記があるが、**そもそもファイルが存在しない**。過去チャットで生成されたが
コミットされなかったものと思われる。再生成時は rev 1.2 の座標で作ること。

座標系の約束: **原点はキー領域の左上（u座標 0,0）／ Y は下方向が正（KiCad と同じ）／ 1u = 19.05mm**。

---

## 6. 作業の進め方

- `split-jis75-tb.html` は単一HTML（インラインCSS/JS、SVGを JS で生成）。
  配列を変えるときは末尾の `LEFT` / `RIGHT` 配列と `circlePad()` を編集する。
  **配列図を変えたら `pcb-spec.md` §5 のマトリクス表と必ず同時に直す**
- 数値を書き換えたら、同じ数値が他のファイルに何箇所あるか `grep` で確認してから閉じる
  （基板外形・パッド中心座標・GPIO割当は3ファイルに重複している）
- rev を上げる変更をしたら、`HANDOFF.md` 冒頭の「最終更新 / rev」と各ファイルのタイトル行を更新する

## 7. コミット

- 日本語のコミットメッセージ
- 設計変更のコミットは**何を変えたかではなく、なぜ変えたか**を書く（§7 の判断根拠に追記できる粒度で）
- push は `main` へ直接で構わない（個人リポジトリ・ブランチ運用なし）
