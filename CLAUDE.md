# CLAUDE.md

自作キーボード **uskn75-kb**（分割JIS 75% + Cirque 35mm トラックパッド）のリポジトリ。
コードベースではなく**ハードウェア設計の資料リポジトリ**である。ビルド・テスト・CI は存在しない。

言語は**日本語**。ドキュメントもコミットメッセージも日本語で書く。

---

## 1. ドキュメントの正本（どれを信じるか）

| 対象 | 正本 |
|---|---|
| 要件・確定仕様・設計判断の根拠・未確定事項・禁止事項・基板外形座標・ピンアサイン・マトリクス | `pcb-spec.md` |
| 現在の進捗ステータス | `README.md` |
| 配列図・設置イメージ・BOM・調達先 | `split-jis75-tb.html` |
| 調達の実務（店舗別リスト・キーキャップ必要数） | `procurement.md` |

**矛盾を見つけたら勝手にどちらかへ寄せず、ユーザーに確認する。**
数値が食い違う箇所は rev の取り違えである可能性が高い（設計 rev 1.9 / PCB仕様 rev 1.3 / HTML は v1.9）。

**進捗の更新は `README.md` の「現在のステータス」に書く**。`pcb-spec.md` は設計内容の変更時にのみ触る。

> かつて `HANDOFF.md`（チャットからの引き継ぎ記録）が要件・判断根拠・未確定事項を持っていたが、
> 2026-08-22 に全内容を `pcb-spec.md` §0/§9〜§13 ほかへ移して廃止した。

---

## 2. 絶対に守ること

`pcb-spec.md` §12 の再掲。これらは要件ではなく**破壊・手戻りに直結する制約**である。

- **Cirque FPC 12ピンの信号順を推測で書かない。** TM035035 のデータシートからの転記のみ。
  「たぶんこの順」で回路図を書くのは禁止。未確定なら未確定と明記して止める
- **Cirque の型番末尾は `-002`。** -003（曲面）/ -001（粘着）は面一埋め込みに使えない
- **キーキャップの寸法を確定する前に PCB を発注しない。** 順序は「キャップ → PCB」
- **半体間 USB-C を PC・充電器に挿さない**（UART と VBUS 5V が乗る）。シルクに明記し、CC に Rd（5.1kΩ）を載せない。
  rev 1.9 で TRRS から替えたので、TRRS の活線挿抜厳禁は無くなった
- **3.3V を半体間で共有しない。** 渡すのは 5V のみ、3.3V は各半体で生成
- Mouser で FFC / Pico / スイッチ類を買わない（国内が安いかフットプリントが合わない）

## 3. 変更してはいけない確定要件

`pcb-spec.md` §0 の R1〜R13 はユーザー発話由来で**変更不可**。
「こうした方が良いのでは」と設計を最適化する前に、その提案が R1〜R13 のどれかを崩さないか確認する。
特に崩しがちなもの:

- **R2: JIS / row-staggered / 75%** — 外出時のノートPC互換性が最優先。列スタガや ANSI 化の提案はしない
- **R7: ポインティングは面一** — トラックボール化の提案は R7 を満たさない（球は必ず突出する）
- **R9: 右Shift 1.75u / ろ 1u** — 刻印キーキャップ互換のため。ここを動かすと JIS 刻印セットが使えなくなる

`pcb-spec.md` §11 に「なぜその選択をしなかったか」が判断ごとに書いてある。
**代替案を出す前に §11 を読む**。そこで既に却下された案を再提案しない
（静電容量無接点 / トラックボール / ポインティングスティック / 23mmパッド / Gen6 / USB左置き / エンコーダ右置き / Duplex-Matrix / 3Dプリントキーキャップ常用）。

---

## 4. 未確定事項の扱い

`pcb-spec.md` §9 の U1〜U15 が未確定。それぞれ**確定方法が決まっている**（データシート / 現物実測 / モック試作 / 実機確認）。

- 未確定項目を**推測で埋めた成果物を作らない**。値が要るなら「U◯ 未確定のため保留」と明記する
- 確定したら `pcb-spec.md` §9 を更新する（かつて HANDOFF と二重管理だったが、一本化済み）
- U1（FPC信号順）は 2026-08-28 に転記済み。**回路図は着手できる**
- U2①（取付穴）と U9（吊り方）は 2026-09-06 に、U11（縦2u のステム位置）は 2026-09-10 に解決。**残りのブロッカーは U2③（全高の基準面）と U12（受け座の固定先）、rev 1.9 で加わった U15（半体間 USB-C のフットプリントと配置）**

---

## 5. 生成物（手で編集しない）

`kle-left.json` / `kle-right.json` / `matrix-left.csv` / `matrix-right.csv` /
`encoders-left.csv` / `keycap-sizes.csv` / `place-left.py` / `place-right.py` は
**`tools/gen-layout.py` の出力**であり、直接編集してはいけない。

```
python3 tools/gen-layout.py
```

このスクリプトは新しい数値を一切持たない。入力は次の2つだけ:

- `split-jis75-tb.html` の `LEFT` / `RIGHT` 配列・`circlePad()`・`jisEnter()`・`encoders()` — 物理配置
- `pcb-spec.md` の §1 キー領域 / §3 パッド開口 / §4 ピンアサイン / §5 マトリクス / §5.5 エンコーダ
  / §5.6 物理行と論理行がずれるキー — 論理配置

両者を突き合わせ、**1つでも食い違えば出力せずエラー終了する**。
したがって配列や仕様を触ったら必ず実行すること（§6 の「両方直す」を機械的に担保するため）。
突合しているのは、行ごとのキー数・キー総数(44/54)・パッド開口中心・エンコーダ中心・
キー領域寸法・エンコーダ押込の行列位置・§5.6 の対応が実在するキーを指しているか。

**物理行は7段、論理 ROW は6段。** 最下段の下の親指Enterは物理行6にあるが、
GPIO を増やさないため隣接する ROW5 の空きセルへ入れてある。この対応は `pcb-spec.md` §5.6 の表が正本で、
スクリプトはそれを読むだけ（スクリプト側に座標や行列の数値を持たせない方針は変えていない）。

座標系の約束: **原点はキー領域の左上（u座標 0,0）／ Y は下方向が正（KiCad と同じ）／ 1u = 19.05mm**。
KLE の左上ラベルは `row,col`（VIA互換・kbplacer が自動検出する形式）。

---

## 6. 作業の進め方

- `split-jis75-tb.html` は単一HTML（インラインCSS/JS、SVGを JS で生成）。
  配列を変えるときは末尾の `LEFT` / `RIGHT` 配列と `circlePad()` を編集する。
  **配列図を変えたら `pcb-spec.md` §5 のマトリクス表と必ず同時に直し、
  `python3 tools/gen-layout.py` を通してから閉じる**（通らなければどちらかが直っていない）
- 数値を書き換えたら、同じ数値が他のファイルに何箇所あるか `grep` で確認してから閉じる
  （基板外形・パッド中心座標・GPIO割当は `pcb-spec.md` と `split-jis75-tb.html` に重複している）
- rev を上げる変更をしたら、`pcb-spec.md` 冒頭の「設計 rev / PCB仕様 rev / 最終更新」と各ファイルのタイトル行を更新する

## 7. コミット

- 日本語のコミットメッセージ
- 設計変更のコミットは**何を変えたかではなく、なぜ変えたか**を書く（`pcb-spec.md` §11 の判断根拠に追記できる粒度で）
- push は `main` へ直接で構わない（個人リポジトリ・ブランチ運用なし）
